# 产品需求与技术实现对齐

更新时间：2026-08-26

本文把当前已经确认的产品规则与代码、云服务器、本地转写和飞书多维表格的职责对齐，作为后续迭代的共同基线。

## 一、最终产品规则

### 监控对象

- 通过抖音号唯一识别账号：`xhls8888`、`569306820`。
- 每个账号可以启用或停用监控。
- 账号名称是展示信息，可以变更；抖音号不能变更。
- 同一个抖音直播间可能出现不同主播，后续需要增加“主要 IP/主播筛选”能力。

### 直播场次

- 开播后立即通知接收人。
- 直播和录制同时进行。
- 下播后算新的一场；上一场转写可以和下一场录制并行。
- 直播记录格式为 `【账号名称】YYYYMMDD_HHMM-HHMM`。
- 每场只生成一条直播记录。

### 内容处理

- 视频必须保存为 MP4，按 15 分钟分段。
- 视频保存在云服务器和本地，不处理原始录像链接。
- 下播后转写从开播到下播的全部录制内容。
- 转写使用本地 Qwen3-ASR；本地电脑关机时任务留在服务器，开机后继续。
- 下播后向接收人发送一张直播截图和一份完整逐字稿。
- 当前不推送 MP4 片段；第一段视频只作为服务器和本地留存素材。

### 配置和发送规则

- 飞书“监控账号列表”维护账号名称、抖音号、监控开关和监控接收人。
- 飞书“直播记录”展示每场任务的状态、失败原因、截图和逐字稿。
- 账号、监控开关、接收人变更不能错误影响已经创建的任务。
- 任务创建时保存账号 ID 和接收人快照。
- 每场、每个接收人、每种消息只发送一次；只重试失败项。
- 手工补录直播记录不能触发新的推送。

## 二、当前实现映射

| 需求 | 当前实现 | 状态 |
| --- | --- | --- |
| 60 秒轮询直播状态 | 云端 `live_digest_service.py` 的 `poll_seconds=60` | 已实现 |
| 两个抖音号并行监控 | 每个 URL 一个线程 | 已实现 |
| 开播通知 | 检测到 `is_live` 后调用飞书应用消息接口 | 已实现，缺少持久化幂等 |
| MP4 分段录制 | FFmpeg segment muxer，默认 900 秒 | 已实现 |
| HLS 失效后恢复录制 | 录制进程退出后重新获取流地址并续录 | 已实现 |
| 下播后形成新场次 | 内存中的 `active/session_stamp` | 已实现，服务重启恢复能力不足 |
| 云端保存录像 | `recordings/<抖音号>/` | 已实现 |
| 本地 Qwen3-ASR | `local_transcriber.py` + Qwen3-ASR | 已实现 |
| 关机后开机继续转写 | 云端 pending manifest + 本地 SSH 拉取 | 已实现 |
| 截图和逐字稿命名 | 本地转写后生成截图、逐字稿 | 已实现，但文件名使用抖音号而非账号名称 |
| 下播推送截图和逐字稿 | 飞书 image/file 消息 | 已实现，缺少按接收人持久化投递账本 |
| 飞书监控账号列表作为配置源 | 当前只读 `live_digest.json` | 未实现 |
| 服务写入直播记录 | 当前没有多维表格写入逻辑 | 未实现 |
| 服务回写账号状态 | 当前没有多维表格状态回写逻辑 | 未实现 |
| 任务账号 ID 快照 | manifest 只保存 `account_id` | 部分实现 |
| 任务接收人快照 | manifest 未保存 recipients | 未实现 |
| 每场每人只发一次 | 仅有本地 published marker，且是整场级别 | 部分实现 |
| 只重试失败接收人/消息 | 当前异常会从整场重新执行 | 未实现 |
| 主要主播/IP 筛选 | 没有主播识别或筛选字段 | 未实现 |

## 三、目标技术方案

### 1. 配置同步层

新增一个配置同步 worker，定时读取飞书多维表格“监控账号列表”，生成本地状态文件：

```text
监控账号列表
      |
      v
config_snapshot.json  <-- 抖音号、账号名称、监控开关、接收人快照
      |
      v
监控 worker
```

规则：

- 抖音号作为唯一键，禁止修改；账号名称只更新展示名称。
- 只有“启用”的账号进入监控集合。
- 配置变更采用版本号或更新时间，worker 在下一轮轮询安全加载。
- 停用账号时，不终止已有录制、转写和推送任务；只阻止创建下一场任务。
- 接收人变更只影响新创建的任务。

### 2. 任务与场次状态

每场直播生成一个不可变 `session_id`，并保存：

```json
{
  "session_id": "20260825_080020",
  "account_id": "xhls8888",
  "account_name": "胡小群讲数学",
  "recipient_snapshot": [
    {"id_type": "user_id", "id": "746d187e", "name": "覃洋"}
  ]
}
```

状态流转：

```text
检查中 -> 直播中/录制中 -> 待转写 -> 转写中 -> 待推送 -> 已完成
                                      \-> 失败（记录失败原因，可重试）
```

直播记录应由任务状态驱动创建和更新，而不是由人工补录触发发送。

### 3. 推送幂等层

新增本地或 SQLite 投递账本，唯一键为：

```text
session_id + recipient_id + message_type
```

`message_type` 至少包括 `live_start`、`screenshot`、`transcript`。

- 发送前写入 `sending`，成功后写入 `sent`，失败写入 `failed` 和错误信息。
- worker 只重试 `failed`，不重试 `sent`。
- 图片成功、逐字稿失败时，只重试逐字稿，不再次发送图片。
- 服务器重启、进程崩溃或网络超时后，按投递账本恢复。
- 飞书返回的 message id 一并保存，便于审计和排障。

### 4. 多维表格同步和回写

服务需要使用飞书 Bitable API：

- 读取监控账号列表：账号名称、抖音号、监控开关、接收人。
- 创建直播记录：一场一行，写入任务快照和 `任务 ID`。
- 更新状态：录制、转写、推送、失败原因、推送时间。
- 上传截图和逐字稿到对应附件字段。
- 只写状态和结果，不因回写动作触发新的推送。

建议保留两个不可见/后台字段：`account_id_snapshot`、`recipient_snapshot`。如果暂时不在表格展示，
至少要保存在 manifest 和投递账本中。

### 5. 主播/IP 筛选

第一阶段只保存抖音号级别监控，避免误把同一直播间不同主播当成同一人。第二阶段增加：

- 主要 IP/主播名称字段；
- 直播画面或标题识别结果；
- `unknown` 状态和人工确认机制；
- 未确认时仍可录制，但按产品配置决定是否推送。

## 四、建议实施顺序

### P0：先修正数据和幂等

1. manifest 增加 `account_name` 和 `recipient_snapshot`。
2. 引入 SQLite 投递账本，拆分截图、逐字稿、开播通知的发送状态。
3. 生成真实直播记录并回写飞书；补录接口必须是显式命令，禁止自动推送。
4. 统一文件名为 `直播视频-账号名称-直播时间-序号.mp4`、`直播截图-账号名称-直播时间.jpg`、
   `直播逐字稿-账号名称-直播时间.txt`。

### P1：接入飞书账号列表

1. 定时读取监控账号列表并生成配置快照。
2. 支持启用/停用的延迟生效规则。
3. 账号和接收人变更只影响新场次。
4. 回写账号服务状态和最近错误。

### P2：完善运营能力

1. 主要主播/IP筛选。
2. 失败任务重试和告警。
3. 磁盘容量、录像保留周期和清理策略。
4. 监控仪表盘和历史数据统计。

## 五、验收标准

- 在表格新增一个启用账号，下一轮同步后开始监控；停用账号不会中断当前场次。
- 同一场直播服务重启一次，仍只产生一条直播记录和每位接收人一条开播通知。
- 截图发送成功、逐字稿发送失败时，重试只发送逐字稿。
- 账号改名后，旧场次保留旧名称，新场次使用新名称。
- 接收人变更后，旧场次按旧快照发送，新场次按新名单发送。
- 下播后直播记录最终包含录制状态、转写状态、推送状态、失败原因、截图和逐字稿。
- 手工补录记录不会调用飞书消息发送接口。

## 六、当前结论

当前系统适合作为“云端监控录制 + 本地转写”的可运行版本，但还不是完整的“飞书多维表格驱动的
监控平台”。下一步不应继续堆叠展示字段，而应优先完成配置同步、直播记录回写、任务接收人快照和
按消息粒度的幂等投递账本。这四项完成后，产品规则才真正由系统保证，而不是依赖人工操作约定。
