# 更新日志

本文件记录每次发布到生产分支的功能调整、问题修复和运维变化。

## [Unreleased]

- 归档视频统一重新编码为 720p/30fps、视频 1.8 Mbps、音频 96 kbps；合并文件通过本地校验后立即清理已参与合并的分段。
- 飞书云盘分片上传改为串行：飞书接口不支持并发上传分片，保留逐片检查点和断点续传。
- 后续改动在合并到 `main` 前先补充到本节；正式发布时移动到对应日期版本。

## [2026-08-28]

### 调整

- 将直播状态轮询和账号配置同步间隔从 60 秒调整为 120 秒，降低不必要的抖音请求；妙记结果轮询仍独立保持 60 秒。

### 新增

- 增加“监控场次”持久化计数，按抖音号保存到服务器状态库。
- 服务启动或同步账号时，将服务状态和监控场次写回飞书“监控账号列表”。

### 修复

- 修复服务重启、账号改名后监控场次被清零的问题。
- 修复首次同步时用较小的表格值覆盖服务器已有计数的问题。
- 修复账号服务状态被误当作当前直播状态的问题：启用账号显示“正常使用”，停用账号显示“未使用”。
- 每次确认新开播只增加一次监控场次，避免轮询重复累加。

### 运维

- 服务器服务已更新并重启，当前运行版本包含上述修复。

## [2026-08-27]

### 录制与可靠性

- 增加录制分片稳定性、可播放性和有效时长检查。
- 录制进程异常退出、无分片更新或直播地址临时失效时自动恢复。
- 合并和上传前增加文件大小及视频时长校验，损坏或缺失内容标记为“部分录制”。
- 完成上传并核对云端字节数后才允许清理服务器 MP4。

### 飞书妙记与消息

- 增加妙记提交锁，防止同一场直播重复创建妙记。
- 按“场次 + 接收人 + 消息类型”持久化去重，失败任务只重试失败目标。
- 直播记录使用账号名称生成文件名和展示名称，并写回录制视频、文字记录和智能纪要链接。

### 发布流程

- 增加活动任务检测和安全发布脚本，避免直播录制过程中直接重启服务。

## 版本说明

- `main` 是生产分支，只有经过验证并明确部署的提交才会影响服务器。
- `develop` 和 `feature/*` 用于开发和测试，不会自动部署。
- 版本提交信息使用动词开头，说明本次变更的结果，例如 `Persist monitoring session counts`。
